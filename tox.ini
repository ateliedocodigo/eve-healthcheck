[tox]
envlist = py27,py3
[testenv]
deps = -rrequirements-dev.txt
commands = coverage erase
           coverage run -m nose -v --with-doctest boilerplate
           coverage report
           coverage html
           pep8 .
